A list item may carry its own {#id} anchor, so an entry in a reference
inventory can be linked to directly rather than by section.

1. Aristotle, _Politics_ {#entry-politics}
1. Plato, _Republic_ {#entry-republic}
1. An item with no marker at all
1. A parent item {#entry-parent}
   1. A nested item keeps its own anchor {#entry-nested}
   1. A nested item without one

- Unordered items work the same way {#entry-unordered}
- Braces {like these} are left alone
- An empty marker {#} stays literal
